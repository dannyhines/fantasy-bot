{
    "files.exclude": {
      "**/*.d.ts": true,
      // Hide all files that end with .js if they have a .ts file
      "**/**/*.js": {
        "when": "$(basename).ts"
      },
      "**/node_modules": true
    },
    "prettier.printWidth": 120
  }